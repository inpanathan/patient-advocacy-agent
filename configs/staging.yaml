# Staging environment configuration

app_env: staging
app_debug: false
use_mocks: false

logging:
  level: INFO
  format: json

server:
  host: "0.0.0.0"
  port: 8000
  reload: false
  workers: 2

llm:
  temperature: 0.3
  timeout_seconds: 30

vector_store:
  persist_dir: data/chroma
  top_k: 10
